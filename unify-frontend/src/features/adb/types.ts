export type AdbResult = {
    id: string
    index:number
    status: string
    startTime: Date
    finishTime: Date
    ipAddress: string
    port: number
    deviceName: string
    result: string
    serviceType: string
}